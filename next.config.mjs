/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['s.h5games.online', 'cdn.htmlgames.com', 'www.onlinegames.io'], // Add this line
      },
};

export default nextConfig;
